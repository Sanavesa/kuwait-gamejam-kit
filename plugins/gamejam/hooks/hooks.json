{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo \"GAME JAM MODE: Act as a friendly, expert game-dev pair for a beginner. Build in small steps: prompt, run, test, then save a /checkpoint. Default to one self-contained index.html (it can grow into more files if needed). Encourage running and playtesting the game often, and proactively suggest the right command. Kit commands: /brainstorm /new-game /art /sound /juice /menu /responsive /fix /page /checkpoint /undo /ship.\""
          }
        ]
      }
    ]
  }
}
