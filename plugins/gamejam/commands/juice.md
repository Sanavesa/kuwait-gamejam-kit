---
description: Add game feel — screen shake, particles, pops, and sound
argument-hint: (optional) which moment to juice, e.g. "when I score"
---
Add "juice" to make the current game feel great. If I named a specific moment in "$ARGUMENTS", focus there; otherwise juice the main actions (scoring, hits, losing).

Add the ones that fit, and keep them tasteful — do NOT break existing gameplay:
- a quick **screen shake** on impactful moments
- a small **particle burst** on hits / pickups
- a **scale pop** (squash & stretch) on the player or key objects
- a brief **hit-pause / freeze-frame** on impact
- a short **sound** on key actions (different sound for success vs. fail)
- a **color flash** on damage or scoring

Use only self-contained code (no external files) — generate simple sounds in code if needed so nothing has to be downloaded. When done, list what you added and remind me to run it, play it, and save a checkpoint if I like it.
